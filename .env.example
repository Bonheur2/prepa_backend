# PostgreSQL connection string (e.g. a Neon pooled connection string)
DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require&channel_binding=require

# Long random string -- generate with: openssl rand -base64 48
JWT_SECRET=replace_with_a_long_random_string

# Groq API key for the AI tutor endpoint
GROQ_API_KEY=replace_with_your_groq_api_key

# Comma-separated allowlist of origins allowed to call this API from a
# browser (e.g. the admin dashboard's deployed URL). Only needed in
# production -- outside production, all origins are allowed by default.
# Not needed for mobile app clients (browsers enforce CORS, mobile apps don't).
CORS_ORIGIN=https://prepa-admin-dashboard.vercel.app
